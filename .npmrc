# npm configuration
# Ensure @microbuild packages go to the public npm registry
@microbuild:registry=https://registry.npmjs.org/

# Hoist next so Amplify SSR deployment can find it in node_modules
public-hoist-pattern[]=next
